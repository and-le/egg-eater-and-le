(fun (make_pair a b)
    (tuple a b)
)

(fun (make_triple a b c)
    (tuple a b c)
)

(let ( (pair (make_pair 1 2)) (triple (make_triple 3 4 5)) (struct (make_pair pair nil) ) ) 
     (block
        (print (index pair 0))
        (print (index pair 1))
        (print pair)
        (print (index triple 0))
        (print (index triple 1))
        (print (index triple 2))
        (print triple)
        (print struct)
     )
)