(fun (fact n)
  (let
    ((i 1) (acc 1))
    (
        if (> i n)
            acc 
            (block
                (set! acc (* acc n))
                (set! n (sub1 n))
                (set! acc (* acc (fact n)))
                acc
            )
    )
  )
)
(fact input)
